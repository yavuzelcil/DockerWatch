Command Structure
=================

dockerwatch list containers # List all running Docker containers
dockerwatch list images     # List all Docker images
dockerwatch start <container_id>  # Start a specific Docker container
dockerwatch stop <container_id>   # Stop a specific Docker container
dockerwatch pull <image_name>      # Pull a Docker image from a registry